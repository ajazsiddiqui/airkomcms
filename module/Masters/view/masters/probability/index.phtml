<?php
$title = $this->translate('Probability');
$this->headTitle($title);
$this->pageBreadcrumbs()->setTitle($title);
$this->mainMenu()->setActiveItemId('masters');
$this->pageBreadcrumbs()->setItems([
            $this->translate(' Probability')=>$this->url('probability'),
            $this->translate('Listings')=>$this->url('probability', ['action'=>'index']),
            ]);

$this->PageActions()->setItems([
            ['label'=>'Add Probability','url'=>$this->url('probability', ['action'=>'add']),'icon'=>'plus','class'=>'btn-primary'],
            ]);	
?>

<div class="row">
  <div class="col-12">
<div class="card">
			<div class="card-header">
				<h4 class="card-title">Manage Probabilities</h4>
			</div>
			<div class="card-body">

<div class="table-responsive">
<table class="table table-responsive-md">
<thead>
<tr>
	<th><strong><?= $this->translate('Id') ?></strong></th>
	<th><strong><?= $this->translate('Name') ?></strong></th>
	<th><strong><?= $this->translate('Status') ?></strong></th>
	<th><strong><?= $this->translate('Date Created') ?></strong></th>
	<th class="fit-width" colspan="2"><strong><?= $this->translate('Actions') ?></strong></th>
</tr>
</thead>
<tbody>
<?php foreach ($probability as $p) : ?>
<tr>
<td><?= $p->getID();?></td>
    <td><a href="<?= $this->url('probability',['action'=>'edit','id' => $p->getId()]); ?>" class="tx-semibold"><?= $p->getName();?></a></td>
  <td><a class="tx-semibold" href="<?= $this->url('probability', ['action'=>'setstatus','id' => $p->getId()]); ?>"><?= ($p->getStatus()==1?'Active':'Deactivated');?></a></td>
	<td><?= $p->getDateCreated();?></td>
	<td>
	<a href="<?= $this->url('probability',[ 'action'=>'edit','id' => $p->getId()]); ?>" class="btn btn-light"><i class="fa fa-pencil"></i>Edit</a>
    </td>
		<td>
		<a href="<?= $this->url('probability',[ 'action'=>'delete','id' => $p->getId()]); ?>" class="btn btn-danger"><i class="fa fa-trash"></i> Delete</a>
    </td>
</tr>
<?php endforeach; ?>
</tbody>
</table></div>


	<?= $this->partial('application/partial/paginator', ['paginator'=>$this->paginator]); ?>

</div>
    </div>
    </div>
    </div>

<?php
$title = $this->translate('Product Models');
$this->headTitle($title);
$this->pageBreadcrumbs()->setTitle($title);
$this->mainMenu()->setActiveItemId('masters');
$this->pageBreadcrumbs()->setItems([
            $this->translate(' Product Models')=>$this->url('product-models'),
            $this->translate('Listings')=>$this->url('product-models', ['action'=>'index']),
            ]);

$this->PageActions()->setItems([
            ['label'=>'Add Product Model','url'=>$this->url('product-models', ['action'=>'add']),'icon'=>'plus','class'=>'btn-primary'],
            ]);	
?>

<div class="row">
  <div class="col-12">
<div class="card">
			<div class="card-header">
				<h4 class="card-title">Manage Product Models</h4>
			</div>
			<div class="card-body">

<div class="table-responsive">
<table class="table table-responsive-md">
<thead>
<tr>
	<th><strong><?= $this->translate('Id') ?></strong></th>
	<th><strong><?= $this->translate('Name') ?></strong></th>
	<th><strong><?= $this->translate('Status') ?></strong></th>
	<th><strong><?= $this->translate('Date Created') ?></strong></th>
	<th class="fit-width" colspan="2"><strong><?= $this->translate('Actions') ?></strong></th>
</tr>
</thead>
<tbody>
<?php foreach ($product_models as $model) : ?>
<tr>
<td><?= $model->getID();?></td>
    <td><a href="<?= $this->url('product-models',['action'=>'edit','id' => $model->getId()]); ?>" class="tx-semibold"><?= $model->getName();?></a></td>
  <td><a class="tx-semibold" href="<?= $this->url('product-models', ['action'=>'setstatus','id' => $model->getId()]); ?>"><?= ($model->getStatus()==1?'Active':'Deactivated');?></a></td>
	<td><?= $model->getDateCreated();?></td>
	<td>
	<a href="<?= $this->url('product-models',[ 'action'=>'edit','id' => $model->getId()]); ?>" class="btn btn-light"><i class="fa fa-pencil"></i></a>
    </td>
		<td>
		<a href="<?= $this->url('product-models',[ 'action'=>'delete','id' => $model->getId()]); ?>" class="btn btn-danger"><i class="fa fa-trash"></i></a>
    </td>
</tr>
<?php endforeach; ?>
</tbody>
</table></div>


	<?= $this->partial('application/partial/paginator', ['paginator'=>$this->paginator]); ?>

</div>
    </div>
    </div>
    </div>
